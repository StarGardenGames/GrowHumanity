enum CIRCLE{
	length,
	start,
	header_size
};

enum THOUGHT{
	need,
	mood,
	total
};

enum EMOTION{
	excited,
	sad,
	happy,
	informing
};

enum EMOTION_PHASE{
	rising,
	holding,
	fading,
	destroy
};

enum HUMAN_STATE{
	wandering,
	informing,
	listening,
	mating,
	deprived
};

enum EDGE_ROW{
	a,
	b,
	weight,
	total
};

enum VIEW_MODE{
	manual,
	auto
};

enum GRID{
	key,
	value
}
